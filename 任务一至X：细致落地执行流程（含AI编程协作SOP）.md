# **工单**

## 任务一至四：细致落地执行流程（含AI编程协作SOP）



#### 任务一：需求分析、前后端架构设计与任务拆解

**核心目标**：确定性设计，杜绝后期“推翻重来”。

- **Step 1.1 需求捕获与结构化**：召开需求宣讲会，使用AI实时转录并生成原始需求池。将模糊描述（如“用户要快”）转化为量化指标（如“列表页首屏加载<1.5s”）。
- **Step 1.2 三方互评（关键）**：组织产品、后端、前端三方会议。**AI角色**：让AI扮演“挑刺专家”，输入需求文档，自动生成“逻辑漏洞清单”（如：未登录状态下购物车添加越界处理），填入《三方需求互评与对齐记录》。
- **Step 1.3 WBS拆解**：使用**MindMap**形式，按“模块-子功能-开发工时”拆解。**AI操作**：输入Epic用户故事，Prompt指令：“按MVC或DDD分层，拆解为粒度<8小时的开发任务，并标注依赖关系”。
- **Step 1.4 技术选型与架构手册**：输出《核心技术架构深度剖析手册》，必须包含：**业务架构图**、**数据流E-R图**、**接口契约规范（RESTful/gRPC）**、**异常处理码映射表**。

------

#### 任务二：基于 AI 编程工具的代码生成、功能实现与测试

**核心目标**：人机结对编程，利用“Loop Engineering”消除幻觉代码。

- **Step 2.1 初始化脚手架**：AI生成 `package.json` / `pom.xml` 及目录结构。
- **Step 2.2 Loop Engineering 精髓（最耗时但最核心）**：
  - **红-绿-重构循环**：先写单元测试骨架（红灯） -> 让AI生成业务代码（绿灯） -> AI分析代码异味并重构。
  - **《自主验证报告》撰写**：每一轮Prompt输入必须附带“上一轮代码运行报错/日志”，AI修正后，记录“初始错误原因 -> 修正策略 -> 最终验证状态”，形成迭代轨迹。
- **Step 2.3 联调证明（两大核心场景）**：选定最复杂的**“多条件组合查询+分页”**和**“高并发下的库存扣减”**。使用 **Postman/APIFox** 导出联调测试集，截图含Request ID和TraceId的完整调用链路，归档为《联调证明》。
- **Step 2.4 防重与单元测试**：针对秒杀/下单接口，AI生成**幂等性测试脚本**（模拟相同RequestId重复提交），确保数据库仅产生一条记录。

------

#### 任务三：打包上线与压力测试

**核心目标**：交付可运行的生产级镜像，并摸清系统极限。

- **Step 3.1 编写Docker-Compose**：必须包含 `app`、`redis`、`mysql`、`nginx` 四个核心容器。配置 `depends_on` 和 `healthcheck`。
- **Step 3.2 容器健康检查与自启动**：编写 `entrypoint.sh`，内含等待数据库就绪的轮询脚本，并设置 `restart: always` + `ulimits` 调优。
- **Step 3.3 Locust 压测脚本编写**：设定**爬坡策略**（从10并发逐步升至500并发）。重点打压任务一/二中定义的两大核心场景。
- **Step 3.4 性能优化对比表**：执行压测 -> 获取第一份数据（如TPS=80）。**AI介入优化**：输入慢SQL日志 -> AI建议添加联合索引 -> 重启压测（TPS升至150）。表格横向对比：**优化前/优化后/提升幅度**。

------

#### 任务四：项目总结与AI代码审查报告

**核心目标**：沉淀资产，不仅做完项目，还要“长本事”。

- **Step 4.1 AI安全与性能审计**：利用 `CodeQL` 或 `SonarQube` 扫描，结合人工Review。AI需重点审查 **SQL注入防护**、**XSS过滤**、**大对象（List）内存泄漏**。输出《AI生成代码安全与性能审计报告》，标注“高危-中危-建议优化”三级。
- **Step 4.2 面试QA库与Prompt模板沉淀**：复盘开发中最折磨人的3个Bug（例如：循环依赖、缓存穿透），AI辅助生成“面试官如何问”与“满分回答话术”。整理出 **《万能Prompt模板库》**（如：写Controller的模板、写事务管理的模板）。